

logging.file: logs/application.log
logging:
  level:
      org:
        springframework:
          web: DEBUG
        hibernate: ERROR

Spring:
  datasource:
    url: jdbc:h2:mem:court
  h2:
    console:
      enabled: true

server:
  port: 8090

